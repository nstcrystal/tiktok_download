# Frontend sẽ gọi backend tại URL này. Để trống = dùng /api (Vite proxy local)
# Khi deploy GitHub Pages (static), set URL backend deploy riêng, ví dụ:
# VITE_API_URL=https://your-backend.onrender.com
VITE_API_URL=

# Backend port (server/index.mjs)
PORT=3001
